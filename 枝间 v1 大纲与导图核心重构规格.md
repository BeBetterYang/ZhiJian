# 枝间 v1 大纲与导图核心重构规格

## 0. Codex 必须先理解的任务性质

这是一次 **Breaking Refactor（破坏性底层重构）**。

项目尚未正式投入使用，因此：

- 不需要兼容旧导图数据
- 不需要历史数据 Migration
- 不需要保留旧 `_outlineId`
- 不需要保留旧 `OutlineNode`
- 不需要兼容旧 localStorage 导图结构
- 不需要为了旧数据保留重复逻辑
- 开发测试数据可以全部清空并重新生成

允许彻底重构：

- 大纲与导图数据模型
- Node ID
- Tree Store
- Undo / Redo
- Markdown Parser / Serializer
- Outline Adapter
- MindMap Adapter
- Persistence
- MindMapEditor 内部架构

但是：

**不要替换 `react-outliner-neo`。**

**不要替换 `SimpleMindMap`。**

它们继续作为两个视图层引擎。

---

# 1. 产品目标

枝间核心不是：

```text
一个思维导图软件
+
一个大纲编辑器
```

而是：

```text
            一份结构化内容
                  │
         ┌────────┴────────┐
         │                 │
       大纲               导图
```

用户编辑的是：

> 一棵结构化思想树。

大纲与导图只是两个不同的编辑视图。

产品体验参考：

```text
幕布
+
Workflowy
+
现代化 SimpleMindMap
```

但保持枝间自己的视觉设计。

---

# 2. 最终核心架构

必须最终形成：

```text
                      ZhiJianDocument
                            │
                            ↓
                     Document Store
                            │
                            ↓
                      Command Engine
                            │
                    Global History
                            │
              ┌─────────────┴─────────────┐
              │                           │
              ↓                           ↓
       Outline Adapter              MindMap Adapter
              │                           │
              ↓                           ↓
    react-outliner-neo              SimpleMindMap
```

严格禁止：

```text
react-outliner
      ⇅
SimpleMindMap
```

直接同步。

也禁止：

```text
Outline Tree
      ⇅
MindMap Tree
```

两份业务 Tree。

---

# 3. 第三方组件职责

## react-outliner-neo

只负责：

```text
大纲显示
文字输入
键盘交互
拖拽交互
节点聚焦 UI
展开折叠 UI
```

不得负责：

```text
业务数据保存
业务 Node ID
全局 Undo
导图同步
数据库数据格式
```

---

## SimpleMindMap

只负责：

```text
导图布局
Canvas
节点渲染
连接线
节点操作 UI
缩放和平移
导图交互
```

不得负责：

```text
Document Source of Truth
全局 Undo
Persistence
Outline 数据
业务数据模型
```

---

# 4. 不再使用 SimpleMindMap 数据作为业务模型

当前类似：

```ts
{
  data: {
    text,
    richText,
    _table,
    _images,
    ...
  },
  children: []
}
```

的 SimpleMindMap 数据，只允许存在于：

```text
MindMap Adapter
```

内部。

业务层禁止引用 SimpleMindMap Node 类型。

---

# 5. 枝间新的统一数据模型

建议采用 **Normalized Tree**。

不要继续以递归对象作为核心状态。

```ts
export type NodeId = string;

export interface ZhiJianDocument {
  id: string;
  title: string;

  rootId: NodeId;

  nodes: Record<NodeId, ZhiJianNode>;

  createdAt: number;
  updatedAt: number;
}
```

节点：

```ts
export interface ZhiJianNode {
  id: NodeId;

  parentId: NodeId | null;

  children: NodeId[];

  content: string;

  blockType:
    | 'text'
    | 'heading1'
    | 'heading2'
    | 'heading3';

  todo?: {
    enabled: boolean;
    checked: boolean;
  };

  note?: string;

  images?: ZhiJianImage[];

  table?: ZhiJianTable;

  style?: ZhiJianNodeStyle;
}
```

---

# 6. 为什么采用 Normalized Tree

不要保存：

```ts
node.children = [
  {
    children: [...]
  }
]
```

作为核心 Store。

采用：

```ts
nodes[id]
```

+：

```ts
children: NodeId[]
```

可以显著简化：

```text
节点移动
查找父节点
拖拽
Undo
删除
ID 管理
循环检测
Selection
性能优化
```

例如：

```ts
nodes['product'] = {
  id: 'product',
  parentId: 'root',
  children: ['user', 'feature'],
  content: '产品',
  blockType: 'text'
}
```

---

# 7. Node ID

所有节点统一：

```ts
crypto.randomUUID()
```

或项目统一 UUID 方法。

禁止：

```text
path
index
timestamp + index
文本内容
```

参与业务 ID。

ID 创建以后永久不变。

---

# 8. Root

每个文件必须存在 Root：

```text
新手入门
```

数据：

```ts
{
  id: '...',
  parentId: null
}
```

Root：

- 永远存在
- 不允许删除
- 不允许拖动
- 不允许缩进
- 不允许改变 parent
- 对应导图中心主题
- 对应 Markdown 第一行标题

---

# 9. content 的定义

`content` 使用：

> Inline Markdown

例如：

```md
**加粗**，*斜体*，<u>下划线</u>，~~删除线~~
```

或者：

```md
[百度](http://baidu.com/)
```

或者：

```md
`this is code`
```

这样：

react-outliner 可以直接编辑 Markdown。

MindMap Adapter 将其转换为 HTML 渲染。

Markdown Export 也可以直接输出。

---

# 10. 不在 content 中保存结构信息

禁止：

```md
- 节点
```

保存在 `content` 中。

`-` 是 Serializer 决定的。

同理：

```md
# 标题
```

中的 `#` 不存入 content。

应该：

```ts
content: '标题'
blockType: 'heading1'
```

---

# 11. 富文本范围

v1 必须支持：

```text
Bold
Italic
Underline
Strike
Inline Code
Link
Font Color
Background Color
Cloze
```

其中：

标准 Markdown 支持：

```md
**bold**
*italic*
~~strike~~
`code`
[link](url)
```

Underline：

```html
<u>underline</u>
```

---

# 12. 字体颜色与背景颜色

标准 Markdown 无法完整表达颜色。

因此：

**颜色属于 ZhiJianDocument 内部样式。**

例如：

```ts
style?: {
  color?: string;
  backgroundColor?: string;
}
```

默认普通 Markdown 导出时：

```text
保留文字
忽略颜色
```

因此用户提供模板中：

```md
- 字体颜色
- 背景颜色
```

保持这种干净格式即可。

未来如需要无损导入导出，再增加：

```text
ZhiJian Extended Markdown
```

本阶段不要污染标准 MD。

---

# 13. 挖空 / Cloze

挖空也是枝间内部语义。

建议数据：

```ts
clozes?: Array<{
  start: number;
  end: number;
}>;
```

或以后独立 Inline Mark。

UI：

```text
你能看见___内容吗？
```

支持点击：

```text
显示 / 隐藏挖空内容
```

普通 Markdown 导出：

```md
- 你能看见挖空内容吗？
```

不额外污染文件内容。

---

# 14. 图片

节点图片：

```ts
images: [
  {
    id,
    url,
    alt,
    width?,
    height?
  }
]
```

Markdown：

```md
- 这是图片
  ![image-1](https://...)
```

图片属于前一个节点的附件。

不能成为独立 Tree Node。

---

# 15. 描述 / Note

幕布式描述属于 Node：

```ts
note?: string
```

Markdown：

```md
- 我有一个描述
  > 这是描述内容
```

UI：

```text
我有一个描述
│ 这是描述内容
```

描述：

- 字体更小
- 颜色更浅
- 左侧细竖线
- 不成为导图子节点
- 不进入 Outline Tree 层级

---

# 16. Todo

Todo 属于节点状态：

```ts
todo: {
  enabled: true,
  checked: false
}
```

Markdown：

```md
- [ ] 1.未完成
- [x] 2.已完成
```

UI：

```text
□ 1.未完成

☑ 2.已完成
```

完成后：

```text
文字降低对比度
删除线
Checkbox checked
```

---

# 17. 表格

表格属于 Node Attachment。

```ts
table?: {
  rows: string[][]
}
```

例如：

```md
表格测试
| 1 | 2 | 3 |
| --- | --- | --- |
| 4 | 5 | 6 |
| 7 | 8 | 9 |
```

Parser 解释为：

```text
节点：表格测试
附件：Table
```

而不是：

```text
表格中的每个 Cell 变成节点
```

---

# 18. Markdown 是什么

重要：

**Markdown 不是枝间运行时唯一数据源。**

运行时 Source of Truth 是：

```text
ZhiJianDocument
```

Markdown 是：

```text
Import / Export Format
```

理由：

标准 Markdown 无法完整表达：

```text
颜色
背景色
Cloze
导图 View State
Canvas 坐标
节点视觉样式
Selection
```

不要为了 Markdown 限制枝间数据模型。

---

# 19. Markdown 输出格式必须固定

用户提供的模板作为基准。

根节点：

```md
# 新手入门
```

第一行唯一代表：

```text
Document Root
```

---

# 20. Root 一级节点

Root 的直接子节点默认不使用 Bullet：

```md
字体测试

颜色测试

表格测试

图片测试
```

这些都是：

```text
Root.children
```

---

# 21. 二级及以下节点

Root 一级节点的 child：

```md
字体测试
- 节点 A
- 节点 B
```

孙节点：

```md
字体测试
- 节点 A
  - 子节点
    - 孙节点
```

统一：

```text
每层 2 Spaces
```

禁止 Tab。

---

# 22. Heading 节点

非常重要：

枝间 Markdown 中的：

```md
# 标题1
## 标题2
### 标题3
```

**代表文字样式，不代表枝间 Tree 层级。**

这是枝间与标准 Markdown Outline 解析最大的不同。

例如：

```md
# 新手入门

# 标题1

## 标题2

### 标题3

正文
```

对应导图：

```text
           新手入门
        /   /   \    \
     标题1 标题2 标题3 正文
```

全部是：

```text
Root children
```

区别只是：

```text
blockType
```

---

# 23. Nested Heading

如果二级节点需要标题样式：

```md
产品设计
- # 大标题
- ## 中标题
- ### 小标题
- 正文
```

Parser：

```ts
blockType = heading1 / heading2 / heading3
```

但 Tree level 仍然由：

```text
Bullet indentation
```

决定。

---

# 24. Markdown Parser 规则

必须重新实现 Parser。

不要继续在现有 Parser 上 Patch。

建议：

```text
src/core/markdown/
    parser.ts
    serializer.ts
    inline.ts
    types.ts
```

Parser 按 Block 顺序解析。

---

# 25. Markdown Parser 核心状态

解析时维护：

```text
root
currentTopLevelNode
currentTreeNode
indentStack
```

不能通过 Markdown Heading Level 推测 Tree 层级。

---

# 26. Markdown Serializer

必须满足：

```text
parse(serialize(document))
```

得到结构等价 Document。

对于标准 MD 支持范围必须 Round Trip。

---

# 27. Markdown Serializer 输出目标

给定：

```text
新手入门
├ 字体测试
│ └ 加粗...
├ 图片测试
│ └ 这是图片
├ 描述测试
│ └ 我有一个描述
└ 待办测试
  ├ 未完成
  └ 已完成
```

输出必须接近：

```md
# 新手入门

字体测试
- **加粗**，*斜体*，<u>下划线</u>，~~删除线~~

图片测试
- 这是图片
  ![image-1](https://example.com/a.png)

描述测试
- 我有一个描述
  > 这是描述内容

待办测试
- [ ] 1.未完成
- [x] 2.已完成
```

---

# 28. 导图视觉总体目标

导图视觉以用户提供的参考图为目标。

关键词：

```text
极简
轻量
白底
线框
大留白
内容优先
无传统彩色气泡
```

不要做成：

```text
XMind 彩色卡片
```

---

# 29. 导图 Root 样式

中心节点：

```text
              新手入门
```

样式：

```text
background: white
border: 1.5px solid #444
border-radius: 8px
padding: 10px 18px
font-size: 20px
font-weight: 400~500
```

不使用强阴影。

---

# 30. 普通节点视觉

默认：

```text
无背景
无外框
文字直接显示在画布
```

不要：

```text
每个 Node 一个卡片
```

只在：

```text
Hover
Selected
Editing
```

时出现轻量背景。

---

# 31. 连接线

参考图：

```text
黑灰色
细线
直角 / 圆角直角
```

推荐：

```text
color: #444
width: 1.5px
```

默认：

```text
rounded orthogonal
```

不使用彩色连接线。

---

# 32. Root 一级连接

SimpleMindMap 使用：

```text
mindMap
```

双侧布局。

自动：

```text
左 / 右
```

平衡分布。

一级节点连接到 Root 主干。

---

# 33. 子节点连接

视觉参考：

```text
      ┌ 子节点A
父节点┤
      └ 子节点B
```

强调：

```text
Bracket / Tree
```

视觉，而不是大量曲线。

---

# 34. 导图留白

整体需要明显比当前传统思维导图更疏。

一级节点：

```text
垂直间距约 42~56px
```

二级：

```text
约 24~34px
```

附件节点根据内容高度自动撑开。

绝对禁止附件相互遮挡。

---

# 35. 字号层级

建议：

Root：

```text
20px
```

Heading1：

```text
19px
font-weight: 600
```

Heading2：

```text
18px
font-weight: 500
```

Heading3：

```text
17px
font-weight: 500
```

Text：

```text
16px
```

Note：

```text
13~14px
```

---

# 36. 富文本视觉

例如：

```text
字体测试 ─── 加粗，斜体，下划线，删除线
```

节点内部直接渲染。

不要另外增加 RichText 卡片。

---

# 37. Link

链接：

```text
百度
```

使用：

```text
蓝色
underline
cursor:pointer
```

点击打开链接。

进入编辑时仍然可以编辑 URL。

---

# 38. Inline Code

例如：

```text
this is code
```

视觉：

```text
浅灰背景
小圆角
等宽字体
红/深灰文字
padding 3px 7px
```

参考用户截图。

---

# 39. Color

字体颜色直接应用文本。

背景颜色只作用于文本：

```text
background highlight
```

而不是整块 Node 卡片。

---

# 40. Table

导图里：

```text
表格测试 ─── [table]
```

表格：

```text
白底
1px #d0d0d0 border
无强阴影
cell padding 8~12px
```

允许直接编辑 Cell。

Table 应属于对应 Node。

---

# 41. Image

参考：

```text
图片测试 ─── 这是图片
                [ image ]
```

图片：

- 在节点正文下方
- 保持原比例
- 设置合理 max-width
- 点击可以查看大图
- 编辑时允许删除/替换
- 不成为一个独立 Tree Node

---

# 42. Note / 描述

视觉：

```text
我有一个描述
│ 这是描述内容
```

使用：

```text
1~2px 左侧竖线
13~14px
muted text
margin-top 4px
```

---

# 43. Todo

参考图：

```text
□ 1.未完成

☑ 2.已完成
```

Checkbox 与文字同一行。

完成状态：

```text
opacity: .45
text-decoration: line-through
```

---

# 44. Cloze

显示状态：

```text
你能看见挖空内容吗？
```

隐藏：

```text
你能看见____内容吗？
```

挖空区域使用：

```text
底部蓝/紫色线
```

可以：

```text
点击显示
再次点击隐藏
```

---

# 45. Hover 状态

默认 UI 尽量隐藏。

Hover Node 时：

出现：

```text
+
⋮⋮
```

或非常轻量的操作入口。

不要常驻大型工具栏。

---

# 46. Selected 状态

选中节点：

```text
background rgba(...)
border-radius: 4~6px
```

不要破坏整张导图极简样式。

---

# 47. Editing 状态

双击节点文字：

直接进入原位编辑。

不要默认弹 Modal。

富文本 Toolbar 可以浮动显示。

---

# 48. 大纲目标

大纲体验参考：

```text
幕布
Workflowy
```

而不是：

```text
传统 Tree Component
```

---

# 49. 大纲整体布局

建议：

```text
max-width: 760~820px
margin: auto
padding-top: 64~80px
```

顶部：

```text
文档标题
```

下方直接进入内容。

---

# 50. 大纲节点

基本视觉：

```text
● 节点内容
    ● 子节点
        ● 孙节点
```

Bullet：

```text
5~7px
```

Indent：

```text
24~28px
```

---

# 51. 大纲默认不显示传统工具栏

不要：

```text
B I U H1 H2 Table...
```

常驻顶部。

采用：

```text
选中文字 → Floating Toolbar
```

和：

```text
节点菜单
```

---

# 52. 大纲快捷操作

必须稳定支持：

```text
Enter
Tab
Shift+Tab
Backspace
ArrowUp
ArrowDown
Alt+Up
Alt+Down
```

---

# 53. Enter

光标末尾：

```text
创建下一个同级
```

光标中间：

```text
拆分节点
```

---

# 54. Tab

当前节点成为：

```text
previousSibling
```

的最后一个 child。

没有 previousSibling：

```text
不处理
```

---

# 55. Shift + Tab

节点提升一级。

提升后位于：

```text
原 parent 后方
```

---

# 56. Backspace

空节点：

```text
删除
```

光标在开头：

```text
与前一可见节点合并
```

---

# 57. Bullet 点击

点击 Bullet：

```text
聚焦当前节点
```

例如：

```text
产品规划
  产品
    Web
    App
```

点击：

```text
产品
```

进入：

```text
产品

● Web
● App
```

---

# 58. Breadcrumb

聚焦后显示：

```text
新手入门 / 产品规划 / 产品
```

点击层级返回。

---

# 59. 展开 / 折叠

拥有 children 时显示：

```text
小型 expand control
```

默认不要一直抢视觉。

Hover 或靠近 Bullet 时增强显示。

---

# 60. 拖拽

大纲拖拽必须支持：

```text
Before
Inside
After
```

并显示清晰 Drop Indicator。

---

# 61. 大纲与导图实时同步

用户在大纲：

```text
创建
删除
编辑
拖动
缩进
提升
Todo
```

必须立即更新 Document Store。

导图从 Store 更新。

---

# 62. 导图操作同理

在导图：

```text
创建
删除
拖动
编辑
Todo
```

必须：

```text
SimpleMindMap Event
      ↓
MindMap Adapter
      ↓
Command
      ↓
Document Store
```

---

# 63. 禁止组件之间直接同步

禁止：

```ts
outlineChange => mindMap.setData()
```

存在于 Outline Component。

也禁止：

```ts
mindMapDataChange => setOutlineData()
```

存在于 MindMap Component。

这些全部由：

```text
Store + Adapter
```

处理。

---

# 64. Command Engine

所有结构修改统一命令：

```ts
createNode()
deleteNode()
moveNode()
updateContent()

setBlockType()

toggleTodo()
setTodoChecked()

setNote()

setImages()
setTable()

setStyle()
```

---

# 65. moveNode

核心 API：

```ts
moveNode({
  nodeId,
  parentId,
  index
})
```

以下全部调用它：

```text
Drag
Tab
Shift+Tab
Move Up
Move Down
```

---

# 66. Tree Validation

必须实现：

```ts
validateDocument()
```

开发环境每次结构操作后验证。

至少检测：

```text
root 是否存在
ID 是否重复
parent 是否存在
children 是否存在
parent/children 是否一致
是否存在环
节点是否多父
Root 是否被移动
```

---

# 67. History

彻底删除：

```text
outlineUndoStack
outlineRedoStack
```

并停止把：

```text
SimpleMindMap BACK/FORWARD
```

作为业务 Undo。

---

# 68. Global History

只有一个：

```text
Document History
```

无论用户当前处于：

```text
大纲
导图
```

Ctrl+Z：

都是撤销整个枝间 Document 的上一操作。

---

# 69. History 必须支持

```text
Create
Delete
Move
Text
Block Type
Todo
Note
Table
Image
Style
```

---

# 70. View 操作不进入 History

例如：

```text
Zoom
Pan
Selection
Hover
View Switch
展开菜单
打开 Dialog
```

---

# 71. 连续文字输入合并

输入：

```text
新手入门
```

不能产生：

```text
新
新手
新手入
新手入门
```

4 个历史记录。

需要：

```text
editing transaction
```

或者：

```text
time-based merge
```

---

# 72. Persistence

Document Store 是保存入口。

不要再依赖：

```text
SimpleMindMap data_change
```

作为自动保存来源。

---

# 73. 自动保存流程

```text
Command
 ↓
Document Store
 ↓
dirty
 ↓
debounce
 ↓
save
```

建议：

```text
500ms
```

---

# 74. 保存内容

服务器保存：

```text
ZhiJianDocument JSON
```

不是：

```text
SimpleMindMap JSON
```

不是：

```text
react-outliner JSON
```

---

# 75. View State 独立保存

另建：

```ts
interface EditorViewState {
  mode: 'outline' | 'mindmap';

  outline: {
    focusNodeId?: NodeId;
    expandedIds: NodeId[];
  };

  mindmap: {
    scale: number;
    x: number;
    y: number;
    expandedIds: NodeId[];
  };
}
```

---

# 76. Document 与 View State 禁止混合

例如：

```text
Canvas X/Y
Zoom
当前 Selection
```

不能出现在：

```text
ZhiJianDocument.nodes
```

---

# 77. 大纲和导图折叠独立

推荐：

```text
Outline expandedIds
```

与：

```text
MindMap expandedIds
```

分开。

因为两种视图的阅读需求不同。

---

# 78. 模式切换

最终：

```ts
setMode('outline')
```

或：

```ts
setMode('mindmap')
```

即可。

禁止：

```text
getData
clone
convert
setData
```

作为模式切换逻辑。

---

# 79. 新目录结构

建议重构为：

```text
src/features/editor/

  core/
    documentTypes.ts
    documentStore.ts
    documentReducer.ts
    documentCommands.ts
    treeUtils.ts
    treeValidation.ts
    history.ts

  markdown/
    parser.ts
    serializer.ts
    inlineMarkdown.ts

  outline/
    OutlineView.tsx
    outlineAdapter.ts
    outlineCommands.ts

  mindmap/
    MindMapView.tsx
    MindMapCanvas.tsx
    mindMapAdapter.ts
    mindMapEvents.ts
    mindMapRenderer.ts

  components/
    FloatingToolbar.tsx
    NodeContextMenu.tsx
    NodeTable.tsx
    NodeImages.tsx
    NodeNote.tsx

  hooks/
    useDocumentPersistence.ts
    useEditorShortcuts.ts

  Editor.tsx
```

---

# 80. 删除旧耦合

重构完成后应该删除：

```text
OutlineNode
_outlineId
pathId
collectNodesById
```

这种旧核心逻辑。

Markdown 模块也不得：

```ts
import type { OutlineNode } from '../OutlineEditor'
```

Core 永远不能依赖 UI。

---

# 81. 依赖方向

必须严格：

```text
UI
 ↓
Adapters
 ↓
Core
```

以及：

```text
Persistence
 ↓
Core
```

绝对禁止：

```text
Core
 ↓
React Component
```

---

# 82. SimpleMindMap Adapter

统一负责：

```ts
documentToMindMapData()
```

以及：

```ts
mindMapOperationToCommand()
```

SimpleMindMap 特殊字段：

```text
data
richText
expand
...
```

只能出现在该目录。

---

# 83. Outline Adapter

统一：

```ts
documentToOutlinerData()
```

以及：

```ts
outlinerChangeToCommands()
```

react-outliner 的：

```text
OutlineData
```

不得离开 outline 模块。

---

# 84. 不要全树同步每一个字符

文本编辑期间：

```text
local editing state
```

允许存在。

在：

```text
blur
Enter
切换节点
合理 debounce
```

时提交 Document Command。

避免：

```text
每一个按键
↓
生成整棵 SimpleMindMap Tree
↓
重新 layout
```

---

# 85. 中文 IME

这是 P0。

必须正确处理：

```text
compositionstart
compositionupdate
compositionend
```

`isComposing === true` 时：

不要响应：

```text
Enter
Backspace
Tab
快捷键
```

Tree 操作。

---

# 86. SimpleMindMap 更新策略

第一阶段允许 Adapter 内：

```text
full setData
```

用于建立正确架构。

但完成稳定架构后：

逐步替换：

```text
Create
Delete
Move
UpdateText
```

为增量操作。

---

# 87. SimpleMindMap 不应该因为大纲输入一个字符而整体重新初始化

尤其禁止：

```text
destroy
new MindMap()
```

和：

```text
重新 mount Canvas
```

---

# 88. 视觉设置

保留当前：

```text
布局
主题
连接线
背景
```

能力。

但是：

默认主题切换成：

```text
枝间极简主题
```

符合参考截图。

---

# 89. 枝间默认导图 Theme

建议：

```ts
{
  backgroundColor: '#FFFFFF',

  root: {
    color: '#333333',
    fillColor: '#FFFFFF',
    borderColor: '#444444'
  },

  node: {
    color: '#3F3F3F',
    fillColor: 'transparent'
  },

  line: {
    color: '#444444'
  }
}
```

---

# 90. 默认结构

新文件默认：

```text
mindMap
```

布局。

连接线：

```text
rounded orthogonal
```

默认：

```text
showNodeBorder = false
```

Root 除外。

---

# 91. 大纲与导图的视觉语义必须一致

例如：

```text
Heading1
```

在大纲：

```text
大号粗体
```

在导图：

```text
大号粗体
```

Todo：

两个视图一致。

Note：

两个视图一致。

颜色：

两个视图一致。

---

# 92. Markdown 语义一致

必须满足：

```text
Document
 ↓ export
Markdown
 ↓ import
Document'
```

对于：

```text
层级
文字
Heading
Todo
Note
Image
Table
Link
Bold
Italic
Underline
Strike
Inline Code
```

结构应保持一致。

---

# 93. 测试基准文件

将用户提供的 Markdown 模板放入：

```text
tests/fixtures/full-feature.md
```

作为核心 Regression Fixture。

必须对应：

```text
tests/fixtures/full-feature.expected.json
```

---

# 94. Markdown Fixture 必须覆盖

```text
Root
Bold
Italic
Underline
Strike
Color fallback
Background fallback
Table
Image
Note
Todo
Link
Inline Code
Cloze fallback
Heading1
Heading2
Heading3
Text
```

---

# 95. 导图视觉测试数据

建立：

```text
full-feature-demo
```

其结构与用户参考图一致。

所有导图 UI 修改必须用它验证。

---

# 96. Tree Unit Tests

必须覆盖：

```text
createNode
deleteNode
moveNode
indentNode
outdentNode
splitNode
mergeNode
reorder
invalid move
cycle prevention
```

---

# 97. Parser Tests

至少：

```text
template MD
↓
Document
```

检查：

```text
Root
Node count
Parent relation
Order
Block type
Todo
Attachment
Note
```

---

# 98. Serializer Tests

必须 Snapshot Test。

保证输出格式不会被未来重构随意改变。

---

# 99. Round Trip Test

```ts
const document2 =
  parseMarkdown(
    serializeMarkdown(document1)
  )
```

检查核心语义相同。

---

# 100. 大纲导图同步测试

流程：

```text
大纲新增节点
↓
导图出现

大纲拖动
↓
导图结构改变

导图新增
↓
大纲出现

导图拖动
↓
大纲结构改变
```

---

# 101. Undo 跨视图测试

```text
Outline:
创建 A

Switch:
MindMap

Undo

A 必须消失
```

再：

```text
Redo
```

A 恢复。

---

# 102. View Switch 压力测试

连续：

```text
Outline ⇄ MindMap
```

100 次。

禁止：

```text
节点增加
节点减少
ID 改变
Undo 改变
数据改变
```

---

# 103. Drag 压力测试

连续进行：

```text
同级排序
移入子节点
提升
跨分支
```

至少 100 次。

最终：

```text
validateDocument() === valid
```

---

# 104. 大数据量

创建：

```text
100
500
1000
3000
```

节点测试。

至少保证：

```text
1000 Node
```

仍能正常大纲输入与切换。

---

# 105. 性能目标

大纲普通输入：

```text
不能明显卡顿
```

节点移动：

```text
视觉响应及时
```

切换视图：

```text
不重新加载服务器数据
```

---

# 106. MindMapEditor 重做原则

允许重新组织甚至重写：

```text
MindMapEditor
```

内部。

不要继续维护 100KB+ 单组件。

最终 Editor 应主要负责：

```text
Store 初始化
View 切换
组件组合
```

---

# 107. 重构策略

因为不考虑数据兼容：

这次不要：

```text
旧架构和新架构长期并存
```

推荐：

### Step A

先建立：

```text
New Core
```

### Step B

接 react-outliner。

### Step C

接 SimpleMindMap。

### Step D

实现 Markdown。

### Step E

验证功能。

### Step F

切换到新 Editor。

### Step G

删除旧：

```text
OutlineNode
old sync
old history
old markdown parser
```

---

# 108. 不要半新半旧

最终禁止出现：

```text
New Document Store
+
old outlineData
+
SimpleMindMap data_change source of truth
```

这种中间态长期存在。

阶段开发可以临时存在。

合并完成后必须删除。

---

# 109. Codex 第一阶段

首先：

**不要直接改 UI。**

先完成：

```text
Core Document Model
+
Document Store
+
Tree Commands
+
History
+
Validation
```

并写测试。

完成后：

```text
STOP
```

汇报结果。

---

# 110. Codex 第二阶段

接入：

```text
react-outliner
```

实现完整幕布式大纲。

完成：

```text
Enter
Tab
Shift+Tab
Backspace
Drag
Focus
Collapse
Todo
Heading
Rich Text
```

并测试。

然后：

```text
STOP
```

---

# 111. Codex 第三阶段

实现：

```text
MindMap Adapter
```

接 SimpleMindMap。

目标：

```text
Document → MindMap
MindMap Events → Commands
```

大纲导图实时同步。

然后 STOP。

---

# 112. Codex 第四阶段

重做：

```text
Markdown Parser
Markdown Serializer
```

严格按照本文件格式。

使用测试 Fixture 验证。

---

# 113. Codex 第五阶段

实现参考截图的：

```text
枝间极简导图 Theme
```

处理：

```text
Root
Lines
Typography
Table
Image
Note
Todo
Code
Link
Cloze
Colors
```

---

# 114. Codex 第六阶段

迁移：

```text
Persistence
```

Document Store 统一自动保存。

删除旧：

```text
SimpleMindMap data_change → save
```

逻辑。

---

# 115. Codex 第七阶段

删除所有旧架构代码。

最终执行：

```text
npm run build
npm run lint
npm test
```

如果当前项目没有 test script：

增加：

```text
Vitest
```

只用于：

```text
Core
Markdown
Adapters
```

不需要本次增加复杂 E2E 框架。

---

# 116. 禁止事项

本次重构绝对禁止：

```text
创建 outlineTree + mindmapTree
```

禁止：

```text
通过 useEffect 双向同步 Tree
```

禁止：

```text
path 作为 Node ID
```

禁止：

```text
index 作为 Node ID
```

禁止：

```text
SimpleMindMap data 作为数据库业务格式
```

禁止：

```text
OutlineData 作为数据库业务格式
```

禁止：

```text
UI Component 定义 Core Model
```

禁止：

```text
Core import React Component
```

禁止：

```text
切换视图时同步数据
```

禁止：

```text
大纲一改就直接 mindMap.setData
```

出现在 Outline UI 层。

禁止：

```text
setTimeout
```

作为数据同步 Bug 的主要解决方案。

禁止：

```text
为了快速通过 TypeScript 大量使用 any
```

---

# 117. 功能优先级

## P0

```text
数据正确性
Node ID
Tree
Undo
Markdown
保存
```

## P1

```text
大纲编辑手感
导图同步
中文输入
拖拽
```

## P2

```text
Note
Todo
Table
Image
RichText
Link
Code
Heading
```

## P3

```text
视觉
动画
Hover
Menu
```

不要反过来。

---

# 118. Definition of Done

只有全部满足以下条件，本次底层重构才算完成。

### Core

- 只有一份 ZhiJianDocument
- Stable UUID
- Normalized Tree
- Tree Validation
- 无循环
- 无重复 ID

### Outline

- react-outliner 正常
- 无限层级
- Enter 正常
- Tab 正常
- Shift+Tab 正常
- Backspace 正常
- Drag 正常
- Focus 正常
- Collapse 正常
- IME 正常

### MindMap

- SimpleMindMap 正常
- 极简主题符合参考图
- 双侧布局正常
- 节点编辑正常
- 拖动正常

### 内容

- Bold
- Italic
- Underline
- Strike
- Heading1
- Heading2
- Heading3
- Text
- Color
- Background
- Todo
- Note
- Image
- Table
- Link
- Inline Code
- Cloze

两个视图都可正确展示。

### Markdown

模板可正确 Import。

Export 输出风格与提供模板一致。

核心语义 Round Trip。

### History

大纲/导图共用一个 Undo/Redo。

跨视图正常。

### Persistence

只保存 ZhiJianDocument。

自动保存正常。

### Architecture

不存在：

```text
outlineData
```

作为第二业务数据源。

不存在：

```text
SimpleMindMap Data
```

作为 Source of Truth。

不存在旧 `_outlineId/pathId`。

Markdown Core 不依赖 OutlineEditor。

MindMapEditor 不再承担所有职责。

---

# 119. 最终产品结构

重构完成后的枝间应该是：

```text
                        枝间
                         │
                         │
                   ZhiJianDocument
                         │
                         │
                   DocumentStore
                         │
                 Command + History
                         │
               ┌─────────┴─────────┐
               │                   │
               │                   │
            大纲模式             导图模式
               │                   │
       react-outliner       SimpleMindMap
               │                   │
               └─────────┬─────────┘
                         │
                       Markdown
                    Import / Export
```

---

# 120. Codex 最终开发原则

这次不要：

> 在旧同步机制上继续修 Bug。

而要：

> 建立一个干净的新 Core，再让两个编辑器接入它。

不要：

> 让 react-outliner 和 SimpleMindMap 互相理解。

而要：

> 两者只理解枝间 Core。

不要：

> 为了保留旧实现继续堆 Compatibility Code。

项目尚未发布：

> 现在就是清理技术债的最佳时间。

最终枝间的核心思想只有一句：

> **内容只有一份，视图可以有很多。**

大纲是 Tree 的纵向表达。

导图是 Tree 的空间表达。

Markdown 是 Tree 的文本表达。

三者必须围绕同一个 ZhiJianDocument 工作。